sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t anajji/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true anajji/docker-react npm run test

